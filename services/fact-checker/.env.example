PORT=3000
GCP_PROJECT_ID=news-lab-2ba2d
GCP_REGION=us-central1

# Path to Firebase service account JSON (local dev only).
# On Cloud Run, omit this — ADC is used automatically via the attached service account.
GOOGLE_APPLICATION_CREDENTIALS=../../credentials/service-account.json

# Optional: augment Gemini analysis with ClaimBuster check-worthiness scoring.
# Register at https://idir.uta.edu/claimbuster/ to get a free API key.
CLAIMBUSTER_API_KEY=
