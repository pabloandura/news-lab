# Server
PORT=3000

# GCP (injected by Cloud Run as env vars)
GCP_PROJECT_ID=news-lab-2ba2d
GCP_REGION=us-central1

# Local development: point to service account JSON
# On Cloud Run this is set automatically via the attached service account
GOOGLE_APPLICATION_CREDENTIALS=../../services/credentials/service-account.json
