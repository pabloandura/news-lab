steps:
  - name: node:20
    id: install-types
    dir: types
    args: ['npm', 'ci']

  - name: node:20
    id: install
    dir: services/polarizer
    args: ['npm', 'ci']

  - name: node:20
    id: typecheck
    dir: services/polarizer
    args: ['npx', 'tsc', '--noEmit']
    waitFor: ['install-types', 'install']

  - name: node:20
    id: test
    dir: services/polarizer
    args: ['npm', 'test']
    waitFor: ['typecheck']

  - name: gcr.io/cloud-builders/docker
    id: build
    args:
      - build
      - -t
      - gcr.io/$PROJECT_ID/polarizer:$COMMIT_SHA
      - services/polarizer
    waitFor: ['test']

  - name: gcr.io/cloud-builders/docker
    id: push
    args: ['push', 'gcr.io/$PROJECT_ID/polarizer:$COMMIT_SHA']
    waitFor: ['build']

  - name: gcr.io/google.com/cloudsdktool/cloud-sdk
    id: deploy
    args:
      - gcloud
      - run
      - deploy
      - polarizer
      - --image=gcr.io/$PROJECT_ID/polarizer:$COMMIT_SHA
      - --region=us-central1
      - --platform=managed
      - --no-allow-unauthenticated
    waitFor: ['push']

# Trigger config:
#   includedFiles: ['services/polarizer/**', 'types/**']
#   branch: ^main$
