steps:
  # Install shared types
  - name: node:20
    id: install-types
    dir: types
    args: ['npm', 'ci']

  # Install service dependencies
  - name: node:20
    id: install
    dir: services/sensemaker
    args: ['npm', 'ci', '--legacy-peer-deps']

  # Validate shared contract
  - name: node:20
    id: typecheck
    dir: services/sensemaker
    args: ['npx', 'tsc', '--noEmit']
    waitFor: ['install-types', 'install']

  # Run tests
  - name: node:20
    id: test
    dir: services/sensemaker
    args: ['npm', 'test']
    waitFor: ['typecheck']

  # Build Docker image (build context = repo root for multi-stage types copy)
  - name: gcr.io/cloud-builders/docker
    id: build
    args:
      - build
      - -f
      - services/sensemaker/Dockerfile
      - -t
      - gcr.io/$PROJECT_ID/sensemaker:$COMMIT_SHA
      - .
    waitFor: ['test']

  # Push to Artifact Registry
  - name: gcr.io/cloud-builders/docker
    id: push
    args: ['push', 'gcr.io/$PROJECT_ID/sensemaker:$COMMIT_SHA']
    waitFor: ['build']

  # Deploy to Cloud Run
  # sensemaker gets higher max instances — ingestion can spike
  - name: gcr.io/google.com/cloudsdktool/cloud-sdk
    id: deploy
    args:
      - gcloud
      - run
      - deploy
      - sensemaker
      - --image=gcr.io/$PROJECT_ID/sensemaker:$COMMIT_SHA
      - --region=us-central1
      - --platform=managed
      - --no-allow-unauthenticated
      - --max-instances=10
      - --service-account=sensemaker-sa@$PROJECT_ID.iam.gserviceaccount.com
      - --set-env-vars=GCP_PROJECT_ID=$PROJECT_ID,GCP_REGION=us-central1
      - --set-secrets=NEWSAPI_KEY=newsapi-key:latest
    waitFor: ['push']

# Trigger config (set in GCP Console or via gcloud):
#   includedFiles: ['services/sensemaker/**', 'types/**']
#   branch: ^main$
